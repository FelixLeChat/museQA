# Build docker image
#  cd docker && docker build -t museplayer .

# Run docker image
#  run -ti -p 8080:8080 -p 5000:5000 museplayer

# Throw data to the recorder
#  ~/Muse/muse-player -f recording.muse -s osc.tcp://45.79.176.71:5000

